# LLM 配置
llm:
  model_name: "llama-3"
  api_key_env: "123"
  base_url: "http://192.168.1.101:60103/v1"
  context_length: 8192
  synth_rules: true

# 数据集配置
dataset:
  name: "incremental_packages_dynamic_capping_subset"
  base_path: "/Data1/hxq/datasets/incremental_packages_dynamic_capping_subset"
  vocab_dir: "vocab_llama3"
  benign_root: "benign"
  malicious_root: "malicious"
  benign_out: "benign_call_processed_llama3"
  malicious_out: "malicious_call_processed_llama3"
  call_graph_filename: "qwen-2.5-coder-32b_call_graph.json"

# 训练配置
training:
  epochs: 60
  batch_size: 128
  learning_rate: 0.005
  weight_decay: 0.001
  patience: 10
  seed: 42
  train_ratio: 0.8
  val_ratio: 0.1
  num_workers: 4

# 模型架构
model:
  hidden_dim: 64
  num_classes: 2
  dropout: 0.7

# 增量学习配置
continual_learning:
  enabled: true
  base_train_months: ['2022-01', '2023-12']
  incremental_months: ['2024-01', '2024-12']
  incremental_epochs: 5
  memory_per_month: 10
  use_memory: True

# 结果文件配置
results:
  future_month: "CL_unk_test_than_train_future_month_llama3.json"

# 路径配置
paths:
  models_dir: "./models_llama3"
  results_dir: "../results_llama3"
  pretrained_model: "./models_llama3/base_model.pt"
  prefix: "incremental_unk_model_llama3_"

# 设备配置 (auto/cuda/cpu)
device: "auto"
